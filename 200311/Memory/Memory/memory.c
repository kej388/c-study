/*
	heap
	: 자동으로 소멸되지 않음 
	→ 개발자가 소멸해줘야 함(free)
	→ free 시키지 않으면 memory leak 발생 ★★★

	heap은 stack과 비교할 때 
	상대적으로 많은 양의 데이터를 효과적으로 처리하기 위해 설계됨

	malloc()
	: 동적메모리 할당. 실행시에 메모리가 할당됨

	static(정적)
	: 실행전에 미리 준비
	
	dynamic(동적)
	: 실행시에 동작
*/

#include <stdio.h>

int main() {
	
}